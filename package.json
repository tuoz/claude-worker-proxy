{
    "name": "claude-worker-proxy",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "deploycf": "wrangler types && npx prettier --write . && wrangler deploy",
        "dev": "wrangler types && npx prettier --write . && wrangler dev --port 8080"
    },
    "devDependencies": {
        "prettier": "^3.6.2",
        "typescript": "^5.5.2",
        "wrangler": "^4.21.0"
    },
    "dependencies": {}
}
